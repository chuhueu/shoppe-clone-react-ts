import React from "react";
import { Footer, HeaderAuth, LoginAuth } from "../components";

const LoginPage = () => {
    return (
        <>
            <HeaderAuth />
            <LoginAuth />
            <Footer />
        </>
    );
};

export default LoginPage;
